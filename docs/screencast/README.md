# Screencast: 429 / Judge unavailable

Drop a short screencast here (`429.mp4`, or replace this file with a hosted link) of the hiring walkthrough when the model returns 429.

What it should show:

1. Open the public studio with no account.
2. Pick the PR review Example and Compile.
3. Walk Skill Spec, Canon preview, Compatibility Report, and Bundle download.
4. On Check, Lint still runs. Judge shows "Judge unavailable" instead of a crash.

Until the recording exists, that sequence is the slot.
